package Select;

import Transform.ParseTable;
import org.json.simple.JSONObject;

import java.util.Collection;
import java.util.stream.Stream;

public class Select {
//    JSONObject select(JSONObject table, Condition condition){
//        Stream<Collection> selectStream = Stream.of(ParseTable.getData(table).values()).filter(record -> condition.isRecordPassCondition((JSONObject) record));
//        return selectStream.toArray();
//    }
}
